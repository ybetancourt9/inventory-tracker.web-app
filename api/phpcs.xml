<?xml version="1.0"?>
<ruleset name="InventoryTrackerApi">
    <description>Coding standard for the Inventory Tracker API.</description>

    <arg name="basepath" value="."/>
    <arg name="colors"/>
    <arg name="extensions" value="php"/>
    <arg value="p"/>

    <file>src</file>
    <file>tests</file>
    <file>config</file>
    <file>bin</file>
    <file>cli-config.php</file>

    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/migrations/*</exclude-pattern>
    <exclude-pattern>*/.phpunit.cache/*</exclude-pattern>
    <exclude-pattern>*/.phpstan.cache/*</exclude-pattern>

    <rule ref="PSR12"/>

    <!--
        Restler's AuthenticationInterface / FilterInterface declare _isAllowed()
        with a leading underscore. Implementing classes have no choice about the
        name, so the sniff is disabled for filters only rather than repo-wide.
    -->
    <rule ref="PSR2.Methods.MethodDeclaration.Underscore">
        <exclude-pattern>src/Api/Filters/*</exclude-pattern>
    </rule>
</ruleset>
